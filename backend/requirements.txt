# Private AI Video Cleaner — backend dependencies (Phase 1)

# API
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
pydantic==2.11.4
pydantic-settings==2.9.1

# Media (wired in Phases 4–8; installed now for environment readiness)
opencv-python-headless==4.11.0.86
numpy==2.2.5
Pillow==11.2.1

# Auth (Phase 3) — declared early for reproducible installs
passlib[bcrypt]==1.7.4
python-jose[cryptography]==3.4.0

# Dev / quality
httpx==0.28.1
pytest==8.3.5
ruff==0.11.8
