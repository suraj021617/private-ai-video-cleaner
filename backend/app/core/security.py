"""Security helpers — full auth implementation lands in Phase 3."""

# Placeholder module so import paths are stable early.
# Phase 3 will add: password hashing, session/JWT creation, cookie helpers.
